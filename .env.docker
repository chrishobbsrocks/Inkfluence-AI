# =============================================================================
# Inkfluence AI - Docker Local Development Environment
# =============================================================================
# Used by docker-compose for local development.
# Uses a local PostgreSQL instance instead of Neon.

# --- Database Driver ---
DB_DRIVER=postgres-js

# --- Database (Local PostgreSQL) ---
DATABASE_URL=postgresql://inkfluence:localdev123@db:5432/inkfluence

# --- Authentication (Clerk) ---
# Replace with your Clerk test keys for full auth testing.
# With placeholders, auth features will not work but the app will start.
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_placeholder
CLERK_SECRET_KEY=sk_test_placeholder
CLERK_WEBHOOK_SECRET=whsec_placeholder
NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in
NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-up

# --- AI ---
ANTHROPIC_API_KEY=sk-ant-placeholder

# --- App Config ---
NEXT_PUBLIC_APP_URL=http://localhost:3000
